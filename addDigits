class Solution {
public:
    int addDigits(int num) {
        int sum = 0;
        while(num) {
            sum += num % 10;
            num = num / 10;
        }
        sum += num;
        return sum >= 10 ? addDigits(sum) : sum;
    }
};
